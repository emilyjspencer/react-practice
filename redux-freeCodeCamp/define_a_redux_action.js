// Define a Redux Action 



const action = {
    type: 'LOGIN'
}


// With Redux, all state updates are triggered by dispatching actions
// A Redux action is a JS object that contains info about an action event that has occurred
// The Redux store receives the action objects and updates its state accordingly
// Define an action by declaring an object with a type property 